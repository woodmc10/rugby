version: '3.8'

services:
  rugby-dev:
    build: .
    container_name: rugby-tackles-dev
    volumes:
      - .:/app
      - ./data:/app/data
      - ./experiments:/app/experiments
    ports:
      - "8888:8888"
    environment:
      - PYTHONPATH=/app
    stdin_open: true
    tty: true
    command: bash

  jupyter:
    build: .
    container_name: rugby-tackles-jupyter
    volumes:
      - .:/app
      - ./data:/app/data
      - ./experiments:/app/experiments
    ports:
      - "8888:8888"
    environment:
      - PYTHONPATH=/app
    command: >
      bash -c "jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root 
               --NotebookApp.token='' --NotebookApp.password=''"